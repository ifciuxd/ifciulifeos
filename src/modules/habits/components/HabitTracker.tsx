import React from 'react';
import GlassCard from '../../../ui/components/layout/GlassCard';

const HabitTracker: React.FC = () => (
  <div className="p-4">
    <h2 className="text-xl font-bold mb-2">Habit Tracker</h2>
    <p className="text-ifciu-textSecondary text-sm">(Placeholder component – implement habit tracking UI here)</p>
  </div>
);

export default HabitTracker;
